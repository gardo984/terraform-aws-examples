## Lambda Functions

Terraform files to create the following:

- Lambda function
- Api Gateway

### Execution
```sh
tofu init
tofu plan
tofu apply -auto-approve
```

### Deletion

```sh
tofu destroy -auto-approve
```